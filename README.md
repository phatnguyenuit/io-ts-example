# io-ts-example

io-ts example which encode/decode data type in the run time.

## Installation

- Using NPM:

  ```sh
  npm install
  ```

- Using Yarn:

  ```sh
    yarn
  ```

## Development

```sh
npx ts-node /path/to/ts/file
```
